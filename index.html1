<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <title>Lời Chúc Năm Mới 🎉</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background: black;
      color: white;
      text-align: center;
    }

    video {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
      z-index: -1;
    }

    .box {
      background: rgba(0, 0, 0, 0.6);
      padding: 20px;
      margin: 40px auto;
      width: 90%;
      max-width: 400px;
      border-radius: 16px;
    }

    input, textarea, button {
      width: 100%;
      margin-top: 10px;
      padding: 10px;
      border-radius: 8px;
      border: none;
      font-size: 16px;
    }

    button {
      background: #e60023;
      color: white;
      font-weight: bold;
      cursor: pointer;
    }
  </style>
</head>

<body>

  <!-- VIDEO NỀN -->
  <video autoplay muted loop>
    <source src="video.mp4" type="video/mp4">
  </video>

  <div class="box">
    <h2>🎊 Lời Chúc Năm Mới 🎊</h2>

    <input id="nameInput" placeholder="Tên của bạn" />
    <textarea id="messageInput" rows="4" placeholder="Nhập lời chúc..."></textarea>

    <button onclick="saveMessage()">💾 Lưu lời chúc</button>
    <button onclick="speakMessage()">🔊 Nghe lời chúc</button>
  </div>

  <!-- FIREBASE -->
  <script type="module">
    import { initializeApp } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-app.js";
    import { getFirestore, doc, setDoc, getDoc } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-firestore.js";

  const firebaseConfig = {
    apiKey: "AIzaSyD9sa4aOaslrlVruI1N8Yb_T3DNw_wL0EY",
    authDomain: "nfc-newyear.firebaseapp.com",
    projectId: "nfc-newyear",
    storageBucket: "nfc-newyear.firebasestorage.app",
    messagingSenderId: "596531180910",
    appId: "1:596531180910:web:b041e5bb43f1ad9d8f7d7f"
  };

    const app = initializeApp(firebaseConfig);
    const db = getFirestore(app);

    // 🔑 LẤY ID TỪ LINK (mỗi link = 1 lời chúc)
    const params = new URLSearchParams(window.location.search);
    const messageId = params.get("id") || "default";

    // 📥 LOAD DỮ LIỆU KHI MỞ TRANG
    async function loadMessage() {
      const ref = doc(db, "messages", messageId);
      const snap = await getDoc(ref);

      if (snap.exists()) {
        const data = snap.data();
        document.getElementById("nameInput").value = data.name || "";
        document.getElementById("messageInput").value = data.message || "";
      }
    }

    loadMessage();

    // 💾 LƯU LỜI CHÚC
    window.saveMessage = async function () {
      const name = document.getElementById("nameInput").value;
      const message = document.getElementById("messageInput").value;

      if (!message) {
        alert("Bạn chưa nhập lời chúc");
        return;
      }

      await setDoc(doc(db, "messages", messageId), {
        name,
        message
      });

      alert("Đã lưu 🎉");
    };

    // 🔊 ĐỌC LỜI CHÚC 
    window.speakMessage = function () {
      const name = document.getElementById("nameInput").value;
      const message = document.getElementById("messageInput").value;

      if (!message) return;

      const text = name ? `${name} chúc: ${message}` : message;
      const utterance = new SpeechSynthesisUtterance(text);
      utterance.lang = "vi-VN";
      speechSynthesis.speak(utterance);
    };
  </script>

</body>
</html>
